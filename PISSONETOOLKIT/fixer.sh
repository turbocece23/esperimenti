#!/bin/bash
GRE='\033[1;32m' #GREEN
RED='\033[1;31m' #RED
NOC='\033[0m'	 #NO COLOR

chmod 755 accesso.sh
chmod 755 fixschermo.sh
chmod 755 intasatore.sh
chmod 755 media.sh
chmod 755 segreto.sh
chmod 755 vez.sh
chmod 755 zucchini.sh

printf "${RED}NOME PROGRAMMA${NOC}	  	---		${RED}STATO${NOC}\n"
printf "${GRE}accesso${NOC}			\tè pronto per l'esecuzione\n"
printf "${GRE}fixschermo${NOC}			è pronto per l'esecuzione\n"
printf "${GRE}intasatore${NOC}			è pronto per l'esecuzione\n"
printf "${GRE}media${NOC}			\tè pronto per l'esecuzione\n"
printf "${GRE}segreto${NOC}			\tè pronto per l'esecuzione\n"
printf "${GRE}vez${NOC}				è pronto per l'esecuzione\n"
printf "${GRE}zucchini${NOC}			è pronto per l'esecuzione\n"
